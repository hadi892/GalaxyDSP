package com.example.pipeline

import com.example.core.ComplexVector

/**
 * Top-level interface for any hardware or file-based digital signal source.
 * Must be interface only.
 */
interface SignalSource : SignalSourceInterface
